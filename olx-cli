#!/bin/sh
set -e
cd "$(dirname "$0")"
if [ -f .venv/bin/python3 ]; then
    PY=".venv/bin/python3"
else
    PY="python3"
fi
if ! "$PY" -c "import click" 2>/dev/null; then
    "$PY" -m pip install -q -r requirements.txt
fi
exec "$PY" -m olx_cli "$@"
